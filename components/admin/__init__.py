"""Админка справочника компонентов — по модулю на раздел главной страницы.

* :mod:`.components` — 27 таблиц компонентов;
* :mod:`.journal` — «Журнал и изображения»: история правок, картинки
  посадочных мест, заявки на рендер;
* :mod:`.option_lists` — «Конструктор выпадающих списков».

Раньше всё это лежало одним файлом на шестьсот строк, и правка
конструктора списков шла вперемешку с формой компонента. Разделы на
главной странице админки задаёт components.admin_site.ComponentsAdminSite
(он подключён через AppConfig); модули здесь повторяют те же разделы.

Django ищет регистрации в ``components.admin`` — поэтому модули
импортируются здесь, иначе ни одна модель в админку не попала бы.
"""

from . import components, journal, option_lists  # noqa: F401
